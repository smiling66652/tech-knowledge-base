# 开发环境配置

> 综合四位UP主内容整理
> 创建时间：2026-04-28

---

## 一、Windows系统优化

### 必装系统工具

| 工具 | 功能 | 来源 |
|------|------|------|
| SpaceSniffer | 磁盘空间可视化 | 小in分享推荐 |
| Everything | 文件极速搜索 | 小in分享推荐 |
| Snipaste | 截图+贴图 | 小in分享推荐 |
| PowerToys | 微软官方增强工具 | 微软官方 |
| Terminal | 现代终端 | 微软官方 |

### C盘清理要点

> 参考：小in分享「C盘清理8个位置」

| 清理位置 | 预计释放 | 风险 |
|----------|----------|------|
| Windows更新缓存 | 2-10GB | 低 |
| 临时文件 | 1-5GB | 低 |
| 浏览器缓存 | 1-3GB | 低 |
| 回收站 | 视情况 | 低 |
| 休眠文件 | 等于内存大小 | 中（关闭休眠） |
| 虚拟内存 | 自定义 | 中 |
| 应用程序缓存 | 视情况 | 低 |
| 旧版本文件 | 视情况 | 低 |

---

## 二、开发工具配置

### 代码编辑器

#### VS Code（主力推荐）

| 必装插件 | 功能 |
|----------|------|
| Python | Python开发支持 |
| Pylance | Python类型检查 |
| GitLens | Git增强 |
| Thunder Client | API测试 |
| Markdown All in One | Markdown编辑 |
| Docker | Docker管理 |
| Error Lens | 错误高亮 |

### 终端环境

#### Windows Terminal

```json
// 配置文件 (profiles.json)
{
  "defaultProfile": "{GUID}",
  "profiles": {
    "list": [
      {
        "name": "PowerShell",
        "source": "Windows.Terminal.PowershellCore",
        "font": { "face": "Cascadia Code", "size": 12 }
      },
      {
        "name": "CMD",
        "commandline": "cmd.exe",
        "font": { "face": "Cascadia Code", "size": 12 }
      }
    ]
  }
}
```

### 版本控制

#### Git 配置

```bash
# 安装Git
# 下载：https://git-scm.com

# 基础配置
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"

# 推荐：设置默认分支名为main
git config --global init.defaultBranch main

# 推荐：配置凭据缓存
git config --global credential.helper manager
```

#### GitHub SSH配置

```bash
# 生成SSH密钥
ssh-keygen -t ed25519 -C "你的邮箱"

# 查看公钥
cat ~/.ssh/id_ed25519.pub

# 添加到GitHub → Settings → SSH Keys
```

---

## 三、Docker环境

### 安装方案

> 参考：技术爬爬虾 DOCKER_INSTALLER

#### 方案A：Docker Desktop（推荐新手）

```
1. 下载Docker Desktop
2. 启用WSL2功能
3. 安装Docker Desktop
4. 配置国内镜像加速
```

#### 方案B：Docker Engine on WSL2

```bash
# WSL2中安装
curl -fsSL https://get.docker.com | sh
sudo service docker start
sudo usermod -aG docker $USER
```

### 国内镜像加速

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
```

### 常用Docker命令

```bash
# 基础操作
docker ps                    # 查看运行中的容器
docker ps -a                 # 查看所有容器
docker images               # 查看镜像
docker pull nginx:latest     # 拉取镜像
docker run -d -p 80:80 nginx # 运行容器
docker stop <id>             # 停止容器
docker rm <id>               # 删除容器

# Docker Compose
docker-compose up -d         # 后台启动
docker-compose down          # 停止并删除
docker-compose logs -f       # 查看日志
```

### 参考项目
- DOCKER_IMAGE_PUSHER：https://github.com/tech-shrimp/docker_image_pusher

---

## 四、WSL2 / Linux环境

### 安装WSL2

```powershell
# PowerShell管理员
wsl --install

# 指定发行版
wsl --install -d Ubuntu-22.04
```

### 基础配置

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装常用工具
sudo apt install -y curl wget git vim htop unzip

# 安装Python
sudo apt install -y python3 python3-pip

# 安装Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs
```

---

## 五、Python环境

### 虚拟环境

```bash
# 创建虚拟环境
python -m venv myenv

# 激活 (Windows)
myenv\Scripts\activate

# 激活 (Linux/Mac)
source myenv/bin/activate

# 安装依赖
pip install -r requirements.txt

# 导出依赖
pip freeze > requirements.txt
```

### 常用包安装

```bash
# Web开发
pip install requests beautifulsoup4 lxml flask fastapi

# 数据处理
pip install pandas openpyxl

# AI相关
pip install openai anthropic google-generativeai

# 自动化
pip install schedule playwright

# 报告生成
pip install jinja2 plotly
```

---

## 六、Node.js环境

### 安装和配置

```bash
# 推荐使用nvm管理Node版本
# Windows: nvm-windows
# Linux/Mac: nvm

nvm install 18
nvm use 18
```

### 全局工具

```bash
# MCP开发
npm install -g @modelcontextprotocol/inspector

# 其他
npm install -g typescript tsx
```

---

## 七、AI工具配置

### Claude Code

```
1. 订阅Claude Pro
2. 安装Claude Code CLI
3. 登录并配置
4. 配置MCP Server（可选）
```

### Claude Desktop + MCP

```json
// 配置文件路径
// Windows: %APPDATA%\Claude\claude_desktop_config.json

{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "C:\\允许的路径"]
    },
    "brave-search": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-brave-search"],
      "env": { "BRAVE_API_KEY": "your_api_key" }
    }
  }
}
```

---

## 八、推荐配置清单

### 开发者环境 Checklist

- [ ] Windows Terminal 配置完成
- [ ] Git安装并配置SSH Key
- [ ] VS Code安装并配置插件
- [ ] Docker Desktop安装并配置镜像加速
- [ ] Python虚拟环境配置
- [ ] Node.js安装（可选）
- [ ] WSL2安装（可选）
- [ ] Claude Code配置（可选）
- [ ] Claude Desktop + MCP配置（可选）

### 效率工具 Checklist

- [ ] Everything（文件搜索）
- [ ] Snipaste（截图）
- [ ] SpaceSniffer（磁盘分析）
- [ ] Obsidian（笔记管理）

---

*本文档将随工具版本更新持续迭代。*
